---
name: execute-ticket
description: Execute one AutoSpec backlog ticket with dependency checks, backlog status updates, QA, and docs updates.
compatibility: opencode
metadata:
  source: skills/claude/execute-ticket.md
  command: /execute-ticket
---

Use this skill when the user wants to run the AutoSpec `execute-ticket` workflow.

- Read `skills/claude/execute-ticket.md` and follow it as the detailed workflow definition.
- Keep `CLAUDE.md` and repo-specific instructions in force.
- Preserve backlog-first, docs-first, and QA-before-done behavior.
- Adapt only the invocation style from Claude slash commands to OpenCode commands/skills.
