# Coding Standards

- camelCase for variables and functions
- PascalCase for React components and types
- snake_case for SQL columns and migration names
- Prefer explicit return types on exported functions
- Avoid business logic in controllers and React pages
- Keep audit and error codes centralized constants
