# Sprint Execution Workflow

- Start with sprint plan prompt and dependency order.
- Mark ticket `in-progress` when work starts.
- Keep backend and frontend parallel only when contracts are explicit.
- Move ticket to `qa-review` after implementation and local verification.
- Record summary, QA result, release notes, and DoD verification at sprint end.
